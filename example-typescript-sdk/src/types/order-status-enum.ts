/**
 * Order Status
 */
export type OrderStatusEnum = "approved" | "delivered" | "placed";

/**
 * @internal
 * Denotes external values of enum (same as above) to stay consistent with other types
 */
export type External$OrderStatusEnum = "approved" | "delivered" | "placed";
